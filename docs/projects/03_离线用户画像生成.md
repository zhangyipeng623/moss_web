# 离线用户画像生成

> 毕业设计项目：MOSS Web 社交媒体舆情模拟推演平台
> 完成人：张艺鹏（个人完成）
> 文档范围：从原始用户/帖子表格到结构化 Agent 画像的离线流水线。

## 1. 功能目标

把用户资料与历史发帖转成可直接驱动 Agent 的结构化画像：长期兴趣、价值立场、表达风格、行为偏好、社交角色与模拟初始状态，为在线推演提供“完整画像种子用户”。

## 2. 输入格式

入口为 `analysis.run_analysis portrait`，读取 `user.xlsx` 与 `post.xlsx`（也可用 CSV，文件名可用 `--user-file`/`--post-file` 覆盖）。

**用户表必需列**：用户名、昵称、简介、性别、地域、关注、粉丝、收藏、创建时间戳（可选：源用户名、用户地址、头像链接）。

**帖子表必需列**：用户名、发文内容、发布时间、发布时间戳、发文类型（可选：点赞数、评论数、转发数、帖子ID）。

## 3. 五阶段 LLM 流水线

`UserPortraitGenerator.run_pipeline` 对每个用户执行 5 个阶段（[user_portrait_generator.py](/Users/zhangyipeng/Documents/moss_web/analysis/user_portrait_generator.py:815)），每阶段独立重试（最多 3 次）：

| 阶段 | 产物 | 说明 |
|---|---|---|
| A 证据抽取 | `EvidencePack` | 帖子分块并行抽取主题、风格、行为、价值观信号与不确定项 |
| B 稳定画像 | `StableProfile` | 长期兴趣、价值立场（value_anchors）、表达风格、社交角色、人口学 |
| C 行为画像 | `BehaviorProfile` | 活跃模式、动作偏好概率、互动偏好、内容偏好、触发规则 |
| D Agent 画像 | `AgentProfile` | 压成单一主人格摘要：身份/兴趣/价值/风格/行为/互动 + 表达/行动/回避规则 |
| E 模拟初始状态 | `SimulationInit` | 情绪、目标、关注话题、立场摘要、注意力目标 |

证据选择按四类排序取并集：最近（recent）、高影响（high_impact）、风格代表（style_representative）、类型代表（type_representative），最多 `max_posts_per_chunk × max_chunks`（默认 120 条）条（[user_portrait_generator.py](/Users/zhangyipeng/Documents/moss_web/analysis/user_portrait_generator.py:1186)）。

## 4. 统计画像与影响力

`analyze_stats` 从用户资料和帖子计算 `UserStats`：粉丝/关注/收藏、发帖数、原创/评论/转发/引用占比、活跃时段、平均互动、帖子影响力（点赞×1 + 评论×2 + 转发×3 加权）与账户影响力（粉丝×0.5 + 帖子影响力×0.3 + 活跃×0.2）（[user_portrait_generator.py](/Users/zhangyipeng/Documents/moss_web/analysis/user_portrait_generator.py:352)）。

## 5. 批量模式与 Rogers 分层

批量模式（`--batch`）分三阶段：先读全表建索引、预计算所有用户的 `account_influence`（不调 LLM）；再用 **P84 单阈值**切分为 L4/L5（Rogers 5 级框架，仅含 Early Adopters + Innovators）；最后调 LLM 生成画像并注入 `influence_tier`（[run_analysis.py](/Users/zhangyipeng/Documents/moss_web/analysis/run_analysis.py:291)）。已存在画像的用户跳过生成、原地更新 tier；失败用户写入 `failed_users.json`。`retier` 子命令可对已有画像目录重新按 P84 分级回写。

## 6. 输出画像结构

`SimulationUserProfile`（版本 `v3-single-persona`）包含：`stats`（统计画像）、`stable_profile`、`behavior_profile`、`agent_profile`、`simulation_init`、`evidence_meta`，以及供模板注入的扁平字段（identity/interest/value/style/behavior/interaction_summary、speaking/action/avoidance_rules_text）。单用户输出 `analysis_outputs/portraits/<user>.json`，批量输出到 `analysis_outputs/portraits/`。

## 7. 当前状态

**已实现**：单用户/批量画像生成、五阶段流水线 + 阶段级重试、活跃时段时区对齐（Asia/Shanghai）、P84 分层与 retier 回写、批量建索引（O(1) 查表）、失败名单落盘。

**待实现**：画像质量人工评估集（跨模型对比、量化准确率）、画像生成离线回归测试、批量任务断点续跑与版本检查、真实用户数据的脱敏治理。
