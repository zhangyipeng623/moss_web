# 论文写作与后续工作待办

> 本文是从已删除的《待实现工作方案》《闭环跑通修复总览》中抽出的**剩余工作**，成为论文阶段的唯一待办清单。全部代码项已实现并验证（`uv run python test.py` 49 项全绿），下方均为**论文写作/表述/研究准备**工作，不再涉及代码改动。
>
> 论文：《融合推荐机制校准的社交仿真平台设计与实现》
> 核心数据约束：观测数据是**横截面的最终传播量**（每条内容一个转发/观看总数），**无时间序列**——此约束决定验证方法与创新点定位。

---

## 一、论文写作阶段工作（代码已实现，待写入论文）

### P1-C 方法描述一致性

论文「方法」章节的描述必须与最终代码实际做法逐条对齐。代码在迭代中已固化如下实现，论文表述需一一对应，避免"代码一套、论文一套"：

- 四维推荐权重由 Optuna 真正搜索（非随机 Dirichlet），`trial.suggest_float` 四维 + `best_params` 回填 + 一致性验证重放；
- 留出验证 70/30（EM 只用训练集，指标在验证集上算）；
- 多指标验证：KS（原始 + 归一化）、Spearman 秩相关、MAE / MRE；
- 多次重复 N≥30 + 95% 置信区间 + Cohen's d；
- 消融实验（逐维去 w_i/w_pop/w_rand/w_time）同时出训练集与验证集两组；
- 鲁棒性审计：`--robustness-seeds` 种子扰动（mean/std/range）；
- 时间单位：`time_scale/3600` 单一真值源，`decay_lambda` 与步长解耦；
- pop 维度归一化：`TIER_WEIGHT` 除以 max（5 级=1.0），`popularity_base` clip ≤1。

### P2-D 非参数检验（Mann-Whitney U + Holm）

鲁棒性审计 / 消融的统计显著性，论文需写明用 **Mann-Whitney U 检验 + Holm 校正**（非参数、多组比较控制族错误率），不只用均值/标准差叙述。代码侧已预留该检验的产出点（`analysis/recommender_parameter_inference.py` 中「Mann-Whitney U + Holm 校正」注释处）。

### P2-F 认识论声明

论文/答辩需诚实声明边界，避免过度声明（对应 017/031 号文献"别过度声明"）：

- **不是逆强化学习**：代码是前向仿真拟合（simulation-based calibration），不是 IRL；MTRec(022)/Beyond Imitation(023) 仅作 related work 的方法启发。
- **不是传播动力学校准**：数据无时间序列，聚焦"**稳态传播规模的分布与排序**"，而非传播过程/时间演化。
- **时间维度弱辨识**：`w_time`/`decay_lambda` 因数据缺时间信息不可辨识。代码保留四维校准不改，但论文声明其为**弱辨识参数、仅作正则项、不作为已校准结论汇报**。

（建议范围声明原文见 [[研究总览_创新点与验证]] 第三节。）

### 画像措辞扰动实验（C-5 收尾）

鲁棒性审计的第二分支：种子扰动已实现，**画像措辞扰动**（改写画像 JSON 的措辞，看校准权重是否稳定）留到实验/论文阶段执行，对应 TRAILS（016 号文献）。

---

## 二、研究准备类待办

来自《下一步对话》与《研究总览》末尾未展开的条目：

1. **公开数据集调研** —— 评估是否有可用公开数据集佐证/对标传播量横截面校准。
2. **先校验参数准不准** —— 用已有数据先验证校准出的四维权重是否合理，再进入大规模实验。
3. （原《下一步对话》第 3 条为空，待补充。）

---

## 三、创新点表述待办（详见 [[研究总览_创新点与验证]]）

- **创新点 1**：解释清楚为什么选「兴趣 / 热度 / 时效 / 随机」这四个可解释维度。
- **创新点 2**：证明大小模型分层"更优"——即"全大模型效果好，但大小模型分层效果差不多、成本很少"。对照实验：**对照组 A 全员大模型**（不写 `llm_small`）vs **实验组 B core 大模型 + mass 小模型**（写 `llm_small`），固定同一批画像 / 随机种子 / `global_event`，只换 mass 的模型；观察传播量分布（KS）、内容排序（Spearman）、token 成本。

---

## 关联

- [[研究总览_创新点与验证]]：创新点、验证方法、文献锚点、可辨识性决策
- [[下一步对话]]：研究准备类待办原始草稿
