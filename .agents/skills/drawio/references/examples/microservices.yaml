meta:
  theme: tech-blue
  layout: horizontal

modules:
  - id: gateway
    label: Gateway
  - id: services
    label: Services
  - id: data
    label: Data Layer

nodes:
  - id: api
    label: API Gateway
    type: service
    module: gateway
    position: { x: 80, y: 80 }
  - id: users
    label: User Service
    type: service
    module: services
    position: { x: 320, y: 80 }
  - id: orders
    label: Order Service
    type: service
    module: services
    position: { x: 320, y: 200 }
  - id: userDb
    label: User DB
    type: database
    module: data
    position: { x: 560, y: 80 }
  - id: orderDb
    label: Order DB
    type: database
    module: data
    position: { x: 560, y: 200 }

edges:
  - from: api
    to: users
    type: primary
  - from: api
    to: orders
    type: primary
  - from: users
    to: userDb
    type: data
    label: CRUD
  - from: orders
    to: orderDb
    type: data
    label: CRUD
  - from: orders
    to: users
    type: optional
    label: Auth Check
